# Exemplo de configuração (NUNCA commite um `.env` com segredos).
#
# 1) Copie este arquivo para `.env` no seu ambiente (local/Colab).
# 2) Ajuste os caminhos conforme sua organização.

# Diretório base de dados usado por `src/hull_tactical/io.py`.
# Deve conter `train.csv`/`test.csv` OU `raw/train.csv`/`raw/test.csv`.
HT_DATA_DIR=/content/drive/MyDrive/hull-tactical/data

# (Opcional) Se rodar o notebook fora do root do repo, você pode apontar o projeto.
# HULL_TACTICAL_PROJECT_ROOT=/content/Contest-Hull-Tactical-Market-Prediction

# Kaggle credentials NÃO são necessárias se você já baixou os dados.
# Se precisar usar o Kaggle CLI manualmente, prefira exportar no ambiente (não no repo).
# KAGGLE_USERNAME=seu_usuario
# KAGGLE_KEY=sua_chave

